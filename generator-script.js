//This is where your JavaScript logic lives 

//Dictionary  
  //declare a variable to hold your words

//Function to generate a random word from the dictionary

/*
  JS has an inbuilt function to radomize data
  An example of it's usage:

  function getRandomInt(min, max) {
    //generates an integer between min(included) and max(included)
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

Function outline and some clues to get you started

function nameYourFunction( doesItTakeAnArgument? ){

  //is there a return statement required? 

}

*/
